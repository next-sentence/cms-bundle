{% if data.translation.image %}
    <img src="{{ data.translation.image.path|imagine_filter(filter|default('sylius_small')) }}" class="ui bordered image"/>
{% else %}
    <img src="http://via.placeholder.com/50x50" class="ui bordered image"/>
{% endif %}