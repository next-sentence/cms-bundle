{% extends '@SyliusShop/layout.html.twig' %}

{% import '@SyliusUi/Macro/messages.html.twig' as messages %}

{% block content %}
    <div class="ui stackable grid segment">
        <div id="lwc-faqs" class="ui sixteen wide column">
            {% if resources|length > 0 %}
                <div class="ui one column stackable grid">
                    {% for frequentlyAskedQuestion in resources %}
                        <div class="lwc-frequently-asked-question">
                            {% include '@LwcCms/Shop/FrequentlyAskedQuestion/Index/_show.html.twig' %}
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                {{ messages.info('sylius.ui.no_results_to_display') }}
            {% endif %}
        </div>
    </div>
{% endblock %}
