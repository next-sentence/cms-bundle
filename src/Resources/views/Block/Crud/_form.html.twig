{% from '@LwcCms/Macro/translationForm.html.twig' import translationForm %}
{% form_theme form '@LwcCms/Form/theme.html.twig' %}

{% include '@LwcCms/Modal/_resourcePreview.html.twig' %}

<div class="ui two column stackable grid">
    <div class="column">
        <div class="ui segment">
            {{ form_errors(form) }}
            {{ form_row(form.code) }}
            {{ form_row(form.enabled) }}
            {{ form_row(form.sections) }}

            <a href="#" class="ui labeled button icon primary lwc-cms-resource-preview" data-url="{{ path('lwc_cms_admin_block_preview') }}">
                <i class="eye icon"></i>
                {{ 'lwc_cms.ui.preview'|trans }}
            </a>
        </div>
    </div>
</div>

<div class="ui one column stackable grid">
    <div class="column" id="lwc-block-image">
        {{ translationForm(form.translations, resource) }}
    </div>
</div>
